workflows:
  umar-horary-android:
    name: Umar Horary Build
    working_directory: .
    environment:
      flutter: stable
    scripts:
      - flutter pub get
      - flutter build apk --release                
    artifacts:                           
      - build/app/outputs/flutter-apk/*.apk